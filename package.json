{
  "name": "testpad-proxy",
  "version": "1.0.0",
  "description": "Proxy server to push test cases to Testpad API (bypasses CORS)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "node-fetch": "^2.7.0"
  }
}
